
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'org.paleozogt:symzip-plugin:0.9.4'
    }
}

plugins {
    id 'base'
}

apply plugin: 'base'
apply plugin: "org.paleozogt.symzip"

dependencies {}

repositories {}

import org.paleozogt.gradle.zip.*;

task dockerBuild(type: Run) {
    doLast {
        run {}
    }
}

assemble.dependsOn dockerBuild
